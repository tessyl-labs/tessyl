If working with voyd code, always read the reference (./.voyd-source/packages/reference). Use idiomatic voyd.

If working with a package that needs a new `@voyd-lang` dependency, make it a
`file` dependency installed from the `./.voyd-source` git submodule.
